/**
 * a singly linked list is a data structure used to store a collection of nodes.
 * and has the following properties
 * it contains a sequence of nodes
 * a node has data and reference to the next node in a list.
 * first node is the head node
 * last node has data and points to null
 * @author issah
 */
public class Main {
    public static void main(String[] args) {
        SinglylinkedList ssl= new SinglylinkedList();
        System.out.println();
    }
}